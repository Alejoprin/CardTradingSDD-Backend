package com.cardtrading.shared.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MailConfig {
    // JavaMailSender is auto-configured by Spring Boot via application.yml mail properties.
    // This class exists as a placeholder for custom mail configuration if needed.
}
